import { TipoEventosPipe } from './tipo-eventos.pipe';

describe('TipoEventosPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoEventosPipe();
    expect(pipe).toBeTruthy();
  });
});
